# Dashboard reorganization blueprint

_Workflow website-reorg-blueprint (wgqzc0emt), 2026-06-06. Audit + judge-panel synthesis._

## Verdict / philosophy

WINNER: Proposal 1 ("The Book, the Edges, the Proof") as the spine, hardened with the two best ideas the others uniquely contribute. Proposal 1 owns the correct top-level inversion (decision-first: Book -> Mechanisms -> Honesty -> Evidence -> Provenance) AND the single most important engineering idea on the table: ROUTE EVERY HEADLINE NUMBER THROUGH RESOLVERS (book_resolver/edges_resolver/honesty_resolver) so the page can never again diverge from knowledge.json — that resolver pattern is what structurally kills the "pending" bug, not a one-off champion-pointer patch. Adopt from Proposal 3 two things it alone gets right: (1) the BOOK COMPOSITIONS & SIZING LAB section that surfaces the ~20 portfolio compositions + per-year strip + leverage dial + alpha-vs-buyhold (the others compute this rich data and throw it away), and (2) the HONEST framing of UUP as a regime *decorrelator* (DSR 0.46, significant=False) rather than a co-equal standalone edge — the data backs P3 here, not the "two ML edges" gloss. Adopt from Proposal 2 the explicit "oil is the NEW chapter" treatment (a NEW badge + the +USO upgrade callout living inside the mechanisms band, and a 6th arc act), and the per-section narrative eyebrow — but REJECT P2's separate standalone "oil" section as redundant sprawl (oil belongs as the 3rd mechanism card + the arc Act VI, not its own band). SYNTHESIZED PHILOSOPHY: "Assert the validated present; make every number a query, not a constant." A cold visitor grasps "book Calmar 4.62, 3 confirmed mechanisms, validated, +USO->5.16 pending" in one viewport / 10 seconds; a quant scrolling 5 minutes reaches every DSR, composition, and round. The page stops spending three sections apologizing for the leaky/stale past (kill the SOXX-pending guard, the STALE_HIST strip, the "USO provisional/overfit-suspect" label) and instead states what survived. One fast page (~140 KB raw), dark research-console aesthetic preserved and modernized.

## Hero

A single above-the-fold BOOK HERO band (id=book), two-column desktop / stacked mobile, replacing BOTH the current status-poller hero AND the broken champions band. LEFT (~68%, the headline): eyebrow "DEPLOYABLE BOOK · leak-free · re-derived 2026-06-06"; a KPI row of large JetBrains-Mono tabular numerals resurrected from the dead _portfolio_html — Calmar 4.62 · Sharpe 2.46 · MaxDD 2.46% · CAGR ~14.6% · positive every yr 2023-26 · 6 names — sourced via book_resolver() from portfolio.proposed_with_USO_2026_06_06.vs_current_6 (4.617/2.46/2.46), NEVER from portfolio.champion (SOXX book). Member chips GLD · UUP · IWM · TIP · DBC · HYG, tinted: teal = carries a confirmed ML edge (GLD only, true standalone), amber-teal = regime decorrelator (UUP), muted = decorrelating buy-hold diversifier (IWM/TIP/DBC/HYG). One-line verdict: "One ML trend edge (GLD) + a dollar-regime decorrelator (UUP) + decorrelated buy-hold diversifiers; weight prop Calmar^2, gross<=1, positive every calendar year." Directly beneath, a distinct UPGRADE CALLOUT pill (amber, .card--upgrade) wired from portfolio.proposed_with_USO_2026_06_06 + next_idea: "PROPOSED — add USO (oil mean-reversion, the 3rd mechanism) -> Calmar 4.62->5.16 (+12%), Sharpe 2.46->2.76, MaxDD 2.46->2.82%. USO(1x) not UCO(2x). Awaiting human/Opus crown." RIGHT (~32%): the live status block (Now-running livedot / Idle idledot + current round, fed by the existing /data.json 8s poller — this is the ONLY surviving home of the poller) stacked over a compact stat tower: "3 confirmed mechanisms · 42/42 fit-prone ETFs screened · 7 honesty lenses". Below the band, one freshness stamp line: "book re-derived 2026-06-06 leak-free · oil arc R1196-1206 · last round 1206". The old single-ticker scoreboard (beat-BH / best-Calmar / luck-check / rounds) is DEMOTED to a thin research-stats strip inside the leaderboard caption — it is not the lead.

## Sections (new IA, in order)

### commandbar — Command bar + thin live strip (primary)
- **Purpose:** Sticky chrome + orient + prove the loop is alive, without stealing the fold.
- **Data source:** build_data().status (live /data.json poll); _scan_rounds_csv()[0] for the one-line latest-callout; nrounds from scoreboard.
- **Components:** Sticky backdrop-blur command bar with the › prompt and chapter anchors UPDATED to the new ids (book·mechanisms·honesty·leaderboard·book-lab·screen·arc·graph·rounds + program.md/deploy). One-line live strip 'live · 1206 rounds · last KEEP: …'. The _latest_callout folds in here as a muted second line. Clock kept.

### book — The deployable book (primary)
- **Purpose:** THE answer in 10s: what to deploy, how good, and the pending upgrade.
- **Data source:** book_resolver() -> portfolio.proposed_with_USO_2026_06_06.vs_current_6 (4.617/2.46/2.46) + .cagr_pct/.members for the current-6 KPIs; portfolio.proposed_with_USO_2026_06_06 (5.16/2.756/2.82) + next_idea for the upgrade pill; build_data().status for the live block.
- **Components:** The BOOK HERO band described above (resurrected _portfolio_html KPIs as .card--book, member chips, verdict line, .card--upgrade USO pill, live status block, freshness stamp).

### mechanisms — Three confirmed edge mechanisms (primary)
- **Purpose:** The mature taxonomy — 'what actually works' — currently rendered NOWHERE.
- **Data source:** edges_resolver(): GLD/UUP from per_etf_best (real_calmar, dsr, psr, significant, trades, config->recipe via describe_cfg); OIL from etf_screen.csv rows USO(2.175/0.937/0.979 STRONG)+UCO(1.102 STRONG)+per_etf_best.XOP(1.626,DSR0.835) joined with next_idea + proposed_with_USO. NOTE: USO/UCO absent from per_etf_best — engineer must add them (data task) or edges_resolver reads screen.csv as the oil source.
- **Components:** Triptych of three .card--edge cards: (1) TREND-MOMENTUM — GLD, Calmar 4.02, DSR 0.94, 602 trades, recipe logdollar×trend_leg+regime_gmm; (2) MACRO-REGIME (decorrelator) — UUP, Calmar 1.30, DSR 0.46, sig=False, framed honestly as 'reads dollar risk-on/off, its job is decorrelation not standalone Calmar', recipe imbalance×bgm+ker; (3) OIL MEAN-REVERSION [NEW badge] — USO/UCO/XOP, DSR ~0.94 tied with GLD, 'fades two-sided oil swings, gate-confirmed real', with the R1196-1206 discover->permute->decay->cost->DSR->book-additive arc as a one-liner. Each card: mechanism, asset(s), Calmar, DSR, gates cleared, one-line 'why it's real'. Footnote names rejected mechanisms (leveraged-equity reversion, sliced_wasserstein artifacts, sticky-HMM predictable-not-profitable, β200=buy-hold pre-filter) so the taxonomy reads as earned.

### honesty — Why these are real — the 7-lens gauntlet (primary)
- **Purpose:** The project's core credibility claim for real money; today only scattered per-row chips.
- **Data source:** per_etf_best.{GLD,UUP,XOP}.{dsr,psr,significant,n_trials} (real, data-backed); permute/decay/cost-stress = qualitative pass from next_idea + confirmed + notes; e_value/pbo = None -> render NA chips (be honest they're uncomputed); gates string from knowledge.json gates; leak verification from autoresearch/BACKTEST_AUDIT.md.
- **Components:** A pass/fail MATRIX: rows = the 3 confirmed edges (GLD/UUP/USO); columns = the 7 lenses (deflated Sharpe, DSR+Holm-Bonferroni, permuted-label control, decay monitor, e-value, PBO, cost stress) as pos/amber/neg/na status chips with the value on hover (GLD DSR 0.94, USO DSR ~0.94, permute collapses to ~-0.09, cost@5bp+dead-bands). One 'backtest is leak-free + fully online' assurance row (ObjectStore-replay-only, online==saved, embargo-bounded) linking BACKTEST_AUDIT.md. The hard-gate floor string (G1 Calmar>3 / G2 trades>80 / G3 no-lookahead / G4 |train-val auc|<0.05) shown as the bar every member cleared. Absorbs the leaderboard 'significance' meaning and the screen selection-bias banner into this one canonical home.

### leaderboard — Single-ticker leaderboard — real OOS, leak-free (secondary)
- **Purpose:** Per-ticker evidence behind the edges; the strongest existing component. Keep, merge charmap in.
- **Data source:** derive(K) over per_etf_best + buyhold + cells (unchanged); _reward_verdict() for the merged 'what it rewards'/verdict columns; scoreboard(K,rows) for the demoted research-stats strip.
- **Components:** Existing _lb_table verbatim (sortable, inline _edgebar bars, _spark Calmar sparklines, signal/val_auc, significance 'holds up/likely luck' chips, plain-English recipe, leak-trust badges, KEEP/DISCARD + buy-hold <details> fold). MERGE _charmap_html in as two added columns / expandable per-row detail ('what it rewards' prose + verdict badge); DELETE _charmap_html + the cmap section. The old scoreboard (beat-BH/best/luck/rounds) becomes a thin research-stats strip in this section's caption. ONE canonical 'how to read the numbers' glossary fold lives at the top here (dedup the two copies). Narrative eyebrow: 'Of 26 ETFs raced, only GLD/UUP/USO clear every gate; the two-sided timing edges (EEM/TLT/IWM) decayed as OOS grew.'

### book-lab — Book compositions & sizing lab (secondary)
- **Purpose:** Surface the rich portfolio data the quant wants — currently computed and discarded. Pure research density (from Proposal 3).
- **Data source:** knowledge.json portfolio.* (equal_weight, conviction_weight, significant_only, decorr_core, conviction_8_with_DBC, with_UUP_9, with_TIP, with_EFA_8(hurts), honest_*, proposed_with_USO_2026_06_06), portfolio.per_year, portfolio.leverage, portfolio.alpha, portfolio.benchmark_buyhold7.
- **Components:** A sortable compositions table: rows = each variant, cols Calmar/Sharpe/MaxDD/CAGR/n/members/note; the current 6-name book row + the proposed +USO row highlighted. Plus a tiny 3-point leverage dial (1x/2x/3x — Calmar-flat, return+DD scale together), the per-year strip (2023 +3.0 / 2024 +8.3 / 2025 +15.5 / 2026 +5.3), and the alpha line (book 4.62 vs passive 1.61). Tells the real decorrelation story (dropping correlated equities lifted Calmar 1.78->3.53).

### screen — Universe screen — the funnel that surfaced oil (secondary)
- **Purpose:** Keep the 311-ETF search machinery for density, but DEMOTE from 'this week's headline' to 'the funnel that found the oil-reversion cluster'.
- **Data source:** load_screen() (etf_screen.csv joined with round_results.csv best axis×labeler), screen_summary(), _mechanism_html breakdown, _excluded_tickers().
- **Components:** Keep the screen table + tier chips + axis/labeler/asset-class breakdown + progress bars + excluded(cash-artifact/NO-BASELINE)/no-fit folds. Reframe lede. FIX the tiering bug: add a VALIDATED tier that overrides the val_auc>0.85->PROVISIONAL heuristic once an edge cleared the honesty stack, so USO (val_auc 0.979) reads 'VALIDATED — high AUC is reversion-label structure, gate-confirmed' not 'overfit-suspect'. CRITICAL: this section's ~20 classes are absent from style.css — must be defined (see design_system). Demote STALE_HIST quarantine strip to one collapsed footnote.

### arc — Research arc & what's next (secondary)
- **Purpose:** Narrative spine + the single most decision-relevant text (next_idea), currently unrendered; merge the two thin story+insights sections.
- **Data source:** _acts_html(K) extended; knowledge.json next_idea (verbatim recommendation); top_insights (filtered to the 2-3 still-true lessons).
- **Components:** A horizontal scroll-snap timeline of 6 acts (extend _acts_html with Act VI 'The third mechanism': oil reversion confirmed on USO/UCO/XOP -> permute/decay/cost/DSR -> proposed into the book; frontier converging 42/42). Each act: title, one sentence, a delta chip (TLT 0.31->1.52, XLE 2.26->0.64, book +USO 4.62->5.16). Revise Act V off 'screen now running'. Render next_idea verbatim as a live 'Current recommendation' callout. Fold the 2-3 still-true methodological lessons (no-lookahead invariant, meta-labeling crosses Calmar>3, selectivity/decorrelation beats exposure) in as act sub-notes; DROP the stale early-grind top_insights list + the standalone Top-insights section.

### graph — Causal / provenance graph (tertiary)
- **Purpose:** Full experiment lineage on demand — not a 517 KB inline tax on every load.
- **Data source:** NEW /graph.json endpoint (nodes/edges/phases from causal_graph) fetched lazily; static counts for the preview; existing causal_graph.html as the full-page link.
- **Components:** Replace the inlined vis dataset with a lightweight preview card (summary: 1284 nodes / 1399 edges / 5 phases + phase legend) + 'open full graph' -> causal_graph.html. Lazy-build vis.Network on the existing IntersectionObserver expand, fetching /graph.json only then. Zero vis dataset in the initial HTML.

### rounds — Rounds ledger (tertiary)
- **Purpose:** Full experiment audit trail, paginated — provenance, not front-page weight.
- **Data source:** _scan_rounds_csv() newest ~25 server-side; NEW /rounds.json?page= for the rest; header count = real CSV total (1206).
- **Components:** Render only newest ~25 rounds server-side (keep KEEP/DISCARD filter chips + recipe gloss + rcal). 'Show all' lazy-fetches the rest from /rounds.json. Stop emitting the ~1180 hidden <li> and the dead deep-link spans for rounds with no backing page. Header reads 'Rounds (1206)'. Optional dynamic /round/<n> route (reads round_results.csv) replaces the 131 stored files for drill-down.

### read — How to read the numbers + colophon (tertiary)
- **Purpose:** One canonical glossary + methods/provenance, closing the page.
- **Data source:** static; links to program.md / deployment.md / BACKTEST_AUDIT.md; global freshness stamp from last_round.
- **Components:** A single 'how to read the numbers' <details> (Calmar/MaxDD/CAGR/Sharpe/Edge/signal/recipe/bar-clock/entry) — the ONLY copy (removed from _intro_html and the leaderboard caption). Short colophon: data sources (knowledge.json, round_results.csv, etf_screen.csv), the gates definition, doc links, global last-updated stamp. The old 'What this is' intro folds into a one-line hero abstract + this colophon.

## Design system

KEEP the identity: palette (--bg #0a0e14, --accent teal #38e0c8, semantic pos/neg/amber with soft+line pairs), dotted-grid + radial-glow background, the › command bar, and rigorously JetBrains Mono + font-variant-numeric:tabular-nums on ALL numerals (the instrument-cluster signature). Keep _spark sparklines + _edgebar diverging bars and reuse them in the new mechanism/honesty/book-lab cards. MODERNIZE:\n1) TYPE SCALE TOKENS. Delete the line 167-168 font-size !important overrides and the html{font-size:112.5%}+body 18px compounding (set base ~16px). Introduce custom props: --fs-display 2.1rem, --fs-h1 1.7rem, --fs-h2 1.3rem, --fs-body .98rem, --fs-mono .95rem, --fs-label .8rem; let the cascade do the work. Add one spacing scale (--sp-1..6) and ONE radius pair (--r 12px, --r-sm 8px) — kill the 12/9/10/hardcoded drift.\n2) ONE .card PRIMITIVE + modifiers. Collapse .stat/.kpi/.champ/.act/.insights-li/.mechcard/.glossary into a single .card (surface var(--card) + 1px var(--line) + var(--r) + optional left-accent stripe) with .card--metric, .card--edge (teal stripe), .card--book (pos stripe), .card--upgrade (amber stripe), .card--mech, .card--muted (flat, no stripe). Teal stripe RESERVED for primary cards only; secondary/tertiary use flatter surfaces so the eye lands book->mechanisms->honesty first.\n3) DEFINE THE ~20 MISSING SCREEN CLASSES (today they render as unstyled browser-default tables on dark bg): tierchips, tc/tc-trust/tc-prov/tc-marg/tc-excl/tc-nofit/tc-tot, scrprog/prow/plabel/pbar/pval, mechcard/mechhd/mechbody/mechnote/scr-mech, tbadge/t-trust/t-prov/t-marg/t-excl/t-nofit, scr-{tier} row tints, aucwarn, ldflag, stalechip, rank, acls, vcell. Map to existing tokens: trust->pos, prov->amber, marginal->amber-soft, excluded->muted+strike-through, nofit->mut, pbar->thin --accent fill on a --bg-2 track, mechcard->.card--mech, tbadge->pill primitive, aucwarn->amber number, ldflag->small accent superscript, stalechip->muted pill. ADD a VALIDATED tier (tc-valid/t-valid->pos) for the USO override. This is the highest visual-quality-per-effort fix on the page.\n4) RESPONSIVE. Replace fixed repeat(4,1fr)/repeat(4,minmax(180px,300px)) grids (.scoreboard/.acts/.champions + the new mechanism triptych + honesty matrix) with repeat(auto-fit,minmax(180px,1fr)); add a ~640px stacked breakpoint so the book KPIs, edge cards, and matrix reflow instead of relying on overflow-x. Sticky table headers (position:sticky;top:command-bar-height) + tighter table line-height 1.3 + zebra interior rules on the long leaderboard/screen/compositions tables; right-align tabular-num metric cols.\n5) PRUNE the near-duplicate greys to a documented token set: keep --ink/--ink-2/--mut/--faint but collapse --line/--line-2 to --line + --line-soft, --bg-2/--card/--card-2 to --bg-2/--card/--card-head; bump --mut on --card past 4.5:1 contrast (eyebrows currently borderline).\n6) FONTS. Drop to 2 families: Space Grotesk (display) + JetBrains Mono (data); IBM Plex Sans -> system-ui body (the swappable one). Self-host or preload+subset with font-display:swap to cut render-blocking first paint (currently 3 Google-hosted families, 11 weights, render-blocking).\n7) Add a small per-section provenance/freshness stamp (last round / timestamp) in --faint so live-vs-historical is visible at a glance. Section eyebrow (one uppercase label per block) carries the narrative beat; hierarchy = SIZE + WEIGHT + the reserved teal stripe.

## render_index.py refactor

render_index.py is a 1042-line monolith mixing file-I/O, CSV parsing, data-derive, ~25 f-string HTML builders, a hardcoded 76-line build_html concat (lines 884-960), and a 70-line CONSOLE_JS string. Restructure into a scripts/console/ package:\nconsole/data.py — ALL load/derive/scan, file-I/O done ONCE per render into a single shared `ctx` dict (today _scan_rounds_csv runs in both build_html and scoreboard, and load_screen runs twice): _load, derive, scoreboard, build_data, _scan_rounds_csv, load_screen, screen_summary, _best_method_join, normalize_series, leak_trust, _excluded_tickers. NO HTML here -> unit-testable. PLUS the three RESOLVERS that are the single source of truth and the core anti-divergence mechanism: book_resolver(K) -> live book metrics from portfolio.proposed_with_USO_2026_06_06.vs_current_6 (never the SOXX champion) + the +USO upgrade; edges_resolver(K) -> GLD/UUP from per_etf_best + oil from screen.csv/next_idea/proposed_with_USO; honesty_resolver(K) -> the 7-lens matrix over per_etf_best dsr/psr/significant/n_trials + qualitative next_idea/confirmed flags. build_data() feeds BOTH the page and /data.json from ctx so render and poll can never diverge.\nconsole/primitives.py — _spark, _edgebar, _num + NEW shared helpers card(), table(), chip(), kpi(), matrix(), eyebrow(), provenance(). A primitive OWNS each CSS class so a renderer can never emit a class the stylesheet doesn't define (the 20-unstyled-screen-class bug becomes structurally impossible).\nconsole/sections/*.py — one pure ctx->HTML builder per section: book.py (resurrect _portfolio_html here), mechanisms.py (NEW), honesty.py (NEW 7-lens matrix), leaderboard.py (_leaderboard_html + merged charmap cols), book_lab.py (NEW compositions/sizing), screen.py (_screen_section_html with the VALIDATED-tier fix), arc.py (acts+insights+next_idea merged), graph.py (preview + lazy), ledger.py (newest-25 + load-more), read.py (glossary+colophon). Each is independently unit-testable (dict in -> fragment out) without Flask.\nconsole/page.py — a DATA-DRIVEN registry SECTIONS=[(id,title,render_fn,priority),…] consumed by build_html, so reorder/relabel is declarative (edit a list, not a 76-line return). Assembles head + command bar + hero + sections + <script src>.\nCROSS-CUTTING: externalize CONSOLE_JS (lines 964-1035) + the vis-network init to a static reports/console.js (cacheable, off the HTML payload); reference via <script src>. Move app.py mid-file imports to top. New app.py endpoints: /graph.json (nodes/edges/phases), /rounds.json?page= (paginated ledger), optional /round/<n> (dynamic drill-down from CSV). INVARIANT to enforce in review: no hardcoded Calmar/Sharpe in any f-string — every headline number flows through a resolver from knowledge.json, so the page can never again show 'pending' or contradict research state. The registry + primitives + resolvers structurally prevent the three recurring bug classes: missing-CSS-class (primitives), dead-code (registry forces every builder referenced), and stale-number (resolvers are the single source).

## Perf plan (fix the 1.3MB page)

The 1.297 MB is two avoidable inline blobs = ~90%; everything else (~120 KB) is fine (sparklines are 3.6 KB; knowledge.json is NOT embedded; style.css is external). This is a pagination problem, not a data-embedding problem.\n1) ROUNDS LEDGER ~620 KB / 50% (_ledger_html_csv serializes all 1206 distinct-timestamp rounds as <li>, ~1180 carry 'hidden extra' but fully download). Render only newest ~25 server-side; 'show all' lazy-fetches the rest from a new /rounds.json?page= via the existing poll plumbing. Stop emitting dead deep-link spans for rounds with no backing page. Saves ~600 KB.\n2) CAUSAL GRAPH ~517 KB / 40% (build_html inlines net_html('cgmain',…) = a 1284-node/1399-edge vis.DataSet with full node titles, downloaded up front; only RENDER is lazy). Stop inlining: ship a static preview card + link to causal_graph.html, and build vis.Network from a new /graph.json on the IntersectionObserver expand that already exists (CONSOLE_JS:1024). Saves ~510 KB.\nThose two land the page at ~120-150 KB raw — pure render_index.py edits, zero data loss (every round still reachable via /rounds.json or /round/<n>; full graph via /graph.json + causal_graph.html).\n3) GZIP. app.py returns Response with no Content-Encoding; index gzips 1.29 MB->111 KB (8.6x) today. Add flask-compress (or a manual gzip in after_request) -> after the two cuts, ~120-150 KB raw -> ~20-30 KB over the wire.\n4) CACHING. The blanket @after_request no-store defeats caching of style.css on every reload AND every 8s poll. Scope no-store to DYNAMIC routes only (/, /index.html, /data.json, /status.json); give style.css + the externalized console.js + graph.json a content-hash or short max-age + ETag so they cache across the poll loop.\n5) EXTERNALIZE the inline CONSOLE_JS + vis init to reports/console.js (cacheable, off the HTML payload).\n6) FONTS. Self-host or preload+subset, drop to 2 families, font-display:swap to cut render-blocking first paint.\nResult: ~140 KB raw / ~25 KB gzipped, fully cached static assets, ledger + graph still reachable on demand. Plus ~5.2 MB disk reclaimed by deleting the 131 legacy pages.

## Cut plan

DISK/SERVED SPRAWL: git rm the 131 reports/round_*.html (5.2 MB — stale Jun-2, only 131 of 1206 rounds covered, each re-embeds its own redundant stale vis graph); git rm reports/_md_legacy/ (round_1..5.md + TEMPLATE.md) and reports/TEMPLATE.html. The live CSV ledger already carries each round's narrative; if drill-down is wanted, replace ALL of them with ONE dynamic /round/<n> Flask route reading round_results.csv (covers all 1206, always fresh, zero stored files). Delete the stale reports/causal_graph.html OR regenerate it live via a route; either way stop inlining the graph. Tighten app.py _ALLOW from ^(round_\\d+[a-z]?|causal_graph|style|status|data)\\.(html|json|css)$ to drop round_\\d+ (pages gone) and data (route-shadowed dead entry), leaving style/status/console.js/graph.json.\nPAGE SECTIONS: DELETE _charmap_html + the 'What each asset rewards' section (merged into leaderboard as columns/expandable rows; _reward_verdict survives as a row helper). DELETE the dead _portfolio_html ONLY AFTER its KPI logic moves into book_resolver()/the Book hero (don't leave unreachable code). MERGE 'The research arc' + 'Top insights' into one arc section; DROP the stale early-grind top_insights (keep 2-3 still-true lessons folded in). DELETE the dead _scan_rounds()/_ledger_html() HTML-scanner fallback path (the CSV path is authoritative). DEDUP the glossary to ONE 'how to read the numbers' fold in the colophon (remove the copy in _intro_html lines 807-821 AND the verbatim repeat in the leaderboard caption lines 914-923). FOLD the old 'What this is' intro into a one-line hero abstract + the colophon; drop the standalone intro block.\nCAUSAL GRAPH: stop inlining net_html('cgmain',…) (517 KB / 40% of the page). Ship a preview card (1284 nodes/1399 edges/5 phases) + link to causal_graph.html; lazy-fetch /graph.json on the existing IntersectionObserver expand.\nDATA-FIX so the page asserts the present instead of quarantining the past: add portfolio.deployed_book_2026_06_06 (the leak-free 6-name GLD/UUP/IWM/TIP/DBC/HYG book, calmar 4.617/sharpe 2.46/mdd 2.46 from vs_current_6) and repoint portfolio.champion to it — this single change turns the hero 'pending' into 4.62 and removes the entire reason the SOXX-strip + champions STALE guard + STALE_HIST quarantine UI exist; demote STALE_HIST to one collapsed footnote. Add USO/UCO (and reconcile XOP's screen NO-FIT vs per_etf_best 1.626) into per_etf_best so the mechanisms/honesty cards have a single data source.\nNET: removes ~5.3 MB disk sprawl, ~1.13 MB page weight (graph+ledger blobs), one full duplicate per-ETF table, two duplicate glossaries, the dead portfolio renderer + dead HTML-scanner path, and the stale/contradictory USO 'provisional' + SOXX-pending UI.

## Implementation steps

1. STEP 0 (data, unblocks everything): in knowledge.json add portfolio.deployed_book_2026_06_06 (members GLD/UUP/IWM/TIP/DBC/HYG; calmar 4.617, sharpe 2.46, mdd_pct 2.46, cagr_pct ~14.6 from proposed_with_USO_2026_06_06.vs_current_6) and repoint portfolio.champion to it; add USO/UCO (and reconcile XOP) into per_etf_best from etf_screen.csv + the oil arc so mechanisms/honesty have one source. Verify python -c loads cleanly.
2. STEP 1 (scaffold): create scripts/console/ package. Move all load/derive/scan/CSV fns into console/data.py building a single ctx dict (file-I/O once); write book_resolver/edges_resolver/honesty_resolver. Add unit tests asserting book_resolver returns calmar 4.617 (never 'pending') and edges_resolver returns 3 mechanisms incl USO.
3. STEP 2 (primitives): create console/primitives.py with _spark/_edgebar/_num + card/table/chip/kpi/matrix/eyebrow/provenance; every helper owns its CSS classes. Port existing markup to use them.
4. STEP 3 (CSS system first — fastest visible win): in style.css introduce the type-scale + spacing + radius tokens, delete the 167-168 !important overrides and the 112.5%/18px compounding, collapse the bespoke cards into .card + modifiers, DEFINE the ~20 missing screen classes + the VALIDATED tier, convert fixed grids to auto-fit + add the 640px breakpoint, add sticky table headers, prune greys, drop to 2 self-hosted fonts. Re-render and eyeball the screen section is now styled.
5. STEP 4 (sections): write console/sections/*.py one builder per final section (book, mechanisms, honesty, leaderboard+merged charmap, book_lab, screen w/ VALIDATED fix, arc w/ Act VI + next_idea, graph preview, ledger newest-25, read/colophon). Delete _charmap_html section, _portfolio_html (after porting), the _scan_rounds/_ledger_html fallback, the duplicate glossaries, the standalone intro + Top-insights sections.
6. STEP 5 (registry): write console/page.py with SECTIONS=[(id,title,fn,priority),…] in the new order (book, mechanisms, honesty, leaderboard, book-lab, screen, arc, graph, rounds, read); build_html assembles head + command bar (anchors updated) + hero + sections + <script src=console.js>. Externalize CONSOLE_JS + vis init to reports/console.js.
7. STEP 6 (perf cuts): in ledger.py render only newest ~25; in graph.py emit the preview card only. Add app.py routes /graph.json, /rounds.json?page=, optional /round/<n>; wire console.js to lazy-fetch both on IntersectionObserver/show-all.
8. STEP 7 (app.py hardening): move imports to top; scope no-store to /,/index.html,/data.json,/status.json; serve style.css/console.js/graph.json with Cache-Control max-age + ETag; add flask-compress (or after_request gzip); tighten _ALLOW to drop round_\d+ and data, add console.js/graph.json.
9. STEP 8 (delete sprawl): git rm the 131 reports/round_*.html, reports/_md_legacy/, reports/TEMPLATE.html, and reports/causal_graph.html (or convert to a live route). Confirm the ledger/graph still reach everything via the new endpoints.
10. STEP 9 (verify): render index.html, assert size < 200 KB (target ~140 KB) and gzip < 30 KB; load in a browser, confirm the book hero shows 4.62 (not 'pending'), the 3 mechanism cards + USO upgrade pill render, the honesty matrix + screen section are styled, the live poller still patches status, the ledger 'show all' + graph lazy-load work, and chapnav anchors resolve. Run the section unit tests + a smoke test of /, /data.json, /graph.json, /rounds.json.

## Risks

DATA GAP: USO/UCO are absent from per_etf_best (only in etf_screen.csv + next_idea/proposed_with_USO), and XOP is screen-verdict NO-FIT but per_etf_best=1.626 — the mechanisms/honesty cards will be inconsistent unless Step 0 reconciles this into one source; don't let edges_resolver silently fall back to a missing key. HONESTY-INTEGRITY: e_value and pbo are None for every ETF and UUP is DSR 0.46 / significant=False — the 7-lens matrix must render genuine NA chips and frame UUP as a decorrelator, NOT paper over gaps with green; overclaiming here would undercut the very credibility the page exists to assert. CHAMPION REPOINT: other tooling may read portfolio.champion — grep the repo before repointing; keep the old honest_book_leakfree key, only move the pointer. BACKTEST coupling: build_data() feeds /data.json and the live poller; the resolver refactor must keep that contract identical or the 8s poll breaks. SCOPE: this is a large refactor touching data + 10 sections + CSS + app.py + perf; land it on a branch behind the existing page, render to a temp file and diff before swapping, and keep each console/ module independently testable so a single bad section can't blank the page. PERF regression guard: add a render-time assert that index.html stays < 200 KB so a future inline-blob can't silently return.
